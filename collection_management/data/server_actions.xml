<odoo>
    <data>
        <record id="action_generate_missing_collection_orders" model="ir.actions.server">
            <field name="name">Generate Collection Orders</field>
            <field name="model_id" ref="ahadubit_property_base.model_property_sale"/>
            <field name="binding_model_id" ref="ahadubit_property_base.model_property_sale"/>
            <field name="state">code</field>
            <field name="code">
# Loop through selected records
for sale in records:
    # Only process confirmed sales that don't have a collection order yet
    if sale.state == 'confirm' and not sale.collection_order_id:
        sale._create_collection_order()
            </field>
        </record>
    </data>
</odoo>