Property Change Log & Bank Search
=================================

Every user who saves a property gets a chatter line (old → new), same style as
unit number tracking. No roles, no separate log menu.

Install / upgrade the module, then upgrade the app list.

Optional: uninstall **track_time_log_timestamp** to avoid duplicate chatter lines.

Bank search on reservation payments: type bank name or account number in the Bank field.
