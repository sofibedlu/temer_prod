<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="0">
        <!-- Approver Group -->
        <record id="group_property_void_request_approver" model="res.groups">
            <field name="name">Void Request Approver</field>
            <field name="category_id" ref="temer_structure.property_mgmt_category"/>
            <field name="implied_ids" eval="[(4, ref('base.group_user'))]"/>
        </record>

        <!-- the Main Menu -->
        <record id="group_contract_team_adjustments" model="res.groups">
            <field name="name">Amendment Void Request for Contract Team</field>
            <field name="category_id" ref="temer_structure.property_mgmt_category"/>
            <field name="implied_ids" eval="[(4, ref('base.group_user'))]"/>
        </record>
    </data>
</odoo>